__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
site/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
tokenbiryani.yaml
*.db
# SQLite's WAL sidecars. *.db does not cover these, so they were committed by
# accident and showed up dirty after every local run.
*.db-shm
*.db-wal
*.sqlite3
# Credential-encryption keys for stored accounts. Never commit one.
*.key
.env
